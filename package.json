{
  "name": "s4-ledger",
  "version": "5.12.0",
  "private": true,
  "type": "module",
  "description": "S4 Ledger — Defense Logistics Blockchain Platform (Monorepo)",
  "scripts": {
    "lint": "eslint prod-app/src/js/ demo-app/src/js/ --max-warnings=0",
    "lint:fix": "eslint prod-app/src/js/ demo-app/src/js/ --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "build:prod": "cd prod-app && vite build",
    "build:demo": "cd demo-app && vite build",
    "build": "npm run build:prod && npm run build:demo",
    "preview:prod": "cd prod-app && vite preview",
    "preview:demo": "cd demo-app && vite preview",
    "ci": "npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@vitest/coverage-v8": "^2.1.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "jsdom": "^24.0.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
