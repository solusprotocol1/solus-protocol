# ═══════════════════════════════════════════════════════════════════════
#  S4 Ledger — Production Environment Variables
#  Copy to .env (local) or set in Vercel dashboard.
# ═══════════════════════════════════════════════════════════════════════

# ── Supabase (REQUIRED for persistence) ──────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key
# JWT secret for validating Supabase Auth tokens server-side
# Find it at: Supabase Dashboard → Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret

# ── API Security (REQUIRED) ─────────────────────────────────────────
# If not set, an ephemeral key is generated per cold start (not stable).
S4_API_MASTER_KEY=your-strong-master-key-here
S4_WEBHOOK_SECRET=whsec_your-strong-webhook-secret-here

# ── Wallet Seed Encryption ───────────────────────────────────────────
# Used to derive Fernet key for encrypting wallet seeds at rest.
# If not set, falls back to SUPABASE_SERVICE_KEY as key material.
# In production, use a dedicated 32+ char random string.
S4_WALLET_ENCRYPTION_KEY=your-strong-encryption-key-here

# ── XRPL Blockchain ─────────────────────────────────────────────────
XRPL_NETWORK=testnet                    # "testnet" or "mainnet"
XRPL_WALLET_SEED=your-issuer-wallet-seed
XRPL_TREASURY_SEED=your-treasury-wallet-seed
XRPL_DEMO_SEED=your-ops-wallet-seed     # Remove after Stripe is live

# ── Stripe Payments ──────────────────────────────────────────────────
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── AI / LLM (at least one required for AI Agent) ───────────────────
# Priority: Azure OpenAI → OpenAI → Anthropic → Fallback
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_KEY=your-azure-key
AZURE_OPENAI_DEPLOYMENT=gpt-4o
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
